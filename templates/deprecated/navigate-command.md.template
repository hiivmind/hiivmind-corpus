---
description: Ask questions about {{project_display_name}} documentation
argument-hint: Your question (e.g., "how does X work?")
allowed-tools: ["Skill"]
---

# {{project_display_name}} Navigator

**User request:** $ARGUMENTS

**Corpus location:** `${CLAUDE_PLUGIN_ROOT}`

---

## If No Arguments Provided

Show a brief help message:

```
{{project_display_name}} Corpus - ask me anything!

Examples:
  /{{plugin_name}}:navigate how does X work?
  /{{plugin_name}}:navigate API reference for Y
  /{{plugin_name}}:navigate getting started guide

Project integration:
  See references/project-awareness.md to add this corpus to your project's CLAUDE.md
```

---

## If Arguments Provided

Invoke the navigate skill to handle the query:

**Skill:** {{plugin_name}}-navigate
**Args:** $ARGUMENTS

The skill will search the index, fetch documentation, and answer the question.
